{
  "name": "hzTouch",
  "version": "0.1.0",
  "description": "alloy touch package",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "./node_modules/.bin/babel src --out-dir dist",
    "prepublish": "npm run compile"
  },
  "author": "hz",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1"
  }
}
